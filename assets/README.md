# Assets

This directory stores static resources:

- `fonts/`: pixel fonts (for example Press Start 2P)
- `icons/`: low-resolution pixel icons
- `sprites/`: UI decoration and future tape/vinyl sprites

The application runs even if these folders are empty.
